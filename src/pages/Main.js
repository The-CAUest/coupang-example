import React from "react";
import TopBar from "../components/TopBar";

const Main = () => (
  <div
    style={{
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
    }}
  >
    <TopBar />
  </div>
);

export default Main;
